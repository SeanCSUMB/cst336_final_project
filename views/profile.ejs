<%- include('partials/header.ejs') %>
        <br>
        <h2><%= currentUsername %>'s Profile</h2>
        <section id="profileInfo">
            <hr>
                
            <a href="/logout"><button class="btn btn-primary">Logout</button></a>
                
            <hr><h3>Change Password:</h3><br><br>
            <form method="POST" action="/changePassword">
                <span class="d-flex justify-content-center">Current Password: &nbsp<input type="password" name="currentPassword" class="input-group-text"></span>
                <br>
                <span class="d-flex justify-content-center">New Password: &nbsp<input type="password" name="newPassword" class="input-group-text"></span>
                <br>
                <span class="d-flex justify-content-center">Retype New Password: &nbsp<input type="password" name="retypeNewPassword" class="input-group-text"></span>
                <br>
                <button class="btn btn-primary">Change Password!</button>
            </form>
        </section>
        <br>
        <h3>Reviewed Products</h3>
        <section id="listContainer">
            <%
                if (Array.isArray(rows)) {
                    rows.forEach(function(row, i) { %>
                        <form><input type='hidden' class='listing' id=<%=row["products_ASIN"] %> name='<%=row["products_ASIN"] %>' value=<%=row["products_ASIN"] %>></form>
                        <div id="listLimiter">
                                <div class="listingContainer"></div>
                        </div>
            <% });}%>
            <% if (rows.length == 0) { %>
                    <h3 class="error">You have not yet made any reviews!</h3>
            <% } %>
        </section>
        <input type="hidden" id="productsLoader" name="productsLoader">
<%- include('partials/footer.ejs') %>                            
<script src="js/profile.js"></script>